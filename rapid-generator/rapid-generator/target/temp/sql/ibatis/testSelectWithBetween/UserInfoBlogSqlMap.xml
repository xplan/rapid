	
	
	
	<select id="UserInfoBlog.testSelectWithBetween" resultMap="RM.UserInfo" >
    <![CDATA[
	select USER_ID,USERNAME,PASSWORD,BIRTH_DATE,SEX,AGE  
	from user_info 
	where birth_date between #minBirthDate# and #maxBirthDate# and username = :specialUsername and sex <= :specialSex and age >= ${specialAge} and password <> #{password}
    ]]>
	</select>	  	
    
	
	
    
